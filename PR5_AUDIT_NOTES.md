# PR #3/#5 audit follow-up

This follow-up records the corrections that can be made safely after the original PRs were already merged.

- `SITE_MAP.md` now treats inline CSS and inline JavaScript as part of the factual theme surface.
- `SITE_MAP.md` no longer treats widget mapping as complete before `config/setup.json` has been fully inventoried.
- `TASKS.md` keeps those remaining Stage 2 analysis items explicitly open.
- `CONTRIBUTING.md` clarifies that `AGENTS.md` controls contributor permissions and that project goals do not authorize unrelated refactoring.

The executable theme code is intentionally unchanged.
